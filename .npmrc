# Silence esbuild build script warnings
enable-pre-post-scripts=true
node-linker=hoisted

# Ignore all build scripts (suppresses warnings)
ignore-scripts=false

# Auto-approve all dependencies
auto-install-peers=true
